# ============================================================================
# Runtime dependencies for the RAG eval harness
# ============================================================================
# All-local stack: LangChain LCEL orchestration + Ollama (LLM & embeddings) +
# pgvector (retrieval) + a HuggingFace cross-encoder (reranking). No cloud LLM
# provider.

langchain-core>=1.2.22,<2
langchain-classic>=1.0.8,<2
langchain-ollama>=1.0,<2
langchain-postgres>=0.0.12,<0.1
psycopg[binary]>=3.1,<4
pgvector>=0.3,<0.4

# Cross-encoder reranker backend (services/rag_chain.py::get_reranker_model)
sentence-transformers>=6.0,<7

httpx>=0.27,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
psutil>=5.9,<8

# Eval harness presentation + lightweight tracking client.  The full `mlflow`
# distribution pulls serving/data-science dependencies that this CLI does not use.
rich>=13.7,<15
mlflow-skinny>=3.15.1,<4
